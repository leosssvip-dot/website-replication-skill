# Local Claude Code / Codex per-user settings
.claude/settings.local.json
.claude/settings.local.*
.codex/settings.local.*

# Audit output — never tracked in the skill repo itself (the skill is shipped
# here, audits run elsewhere). For the recommended .gitignore policy in the
# consumer project that hosts real audit output, see SKILL.md.
audit/

# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/
*.swp
*.swo
*~

# Misc
.env
.env.*
